# supply-chain cooldown (npm >= 11.10): refuse package versions younger
# than 3 days, so compromised releases get caught and pulled before we
# can resolve them. only applies when changing deps; installs from the
# committed lockfile are unaffected.
min-release-age=3
